{
  "name": "octopus-model",
  "version": "1.0.5",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "tsc && tsc -w",
    "test": "gulp test",
    "compile": "tsc",
    "tsc:w": "tsc -w",
    "clean": "rimraf bundles coverage index.js.map index.d.ts src/**/*.map src/**/*.d.ts src/**/*.metadata.json src/**/*.js settings/**/*.d.ts settings/**/*.metadata.json settings/**/*.js index.d.ts index.metadata.json index.js",
    "build": "webpack",
    "doc": "typedoc --theme minimal --mode file --out ./doc ./index.ts"
  },
  "directories": {
    "test": "tests"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tralalere/octopus-model.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/tralalere/octopus-model/issues"
  },
  "homepage": "https://github.com/tralalere/octopus-model#readme",
  "devDependencies": {
    "chai": "^4.1.2",
    "gulp": "^3.9.1",
    "gulp-mocha": "^4.3.1",
    "rimraf": "^2.6.2",
    "ts-loader": "^3.0.5",
    "typescript": "^2.5.3",
    "webpack": "^3.8.1"
  },
  "dependencies": {
    "systemjs": "^0.20.19",
    "typedoc": "^0.10.0"
  }
}
